generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "mysql"
    url      = env("DB_URL")
}

model Ingredient {
    id Int @id @unique
    name String
    unit String
    calories Int
}